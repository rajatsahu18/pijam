import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'lms.example.app',
  appName: 'lms',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
